module.exports = (client, message) => 
{
    // if(message.author == client.user)
    //     return message.channel.send(message.content)
    // else
    // {
    //     message.channel.send(`Message from ${message.author.tag} was deleted! Content was:\n${message.content}`)
    // }
}